app.name=oncase-mondrian-security
prj.version=0.1.0
src.dir=src
classes.dir=classes
lib.dir=lib
dist.dir=dist
jar.name=${app.name}-${prj.version}.jar
jar.file=${dist.dir}/${jar.name}
test.file=/opt/pentaho512ee/server/biserver-ee/tomcat/webapps/pentaho/WEB-INF/lib/${jar.name}
